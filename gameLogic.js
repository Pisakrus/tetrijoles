// ---Tetris---

// Creates a row with n_columns of 0
let row = [];
for (let i = 0; i < window.N_COLUMNS; i++) row.push(0);

// Creates grid with n_rows of rows
let grid = [];
for (let i = 0; i < window.N_ROWS; i++) grid.push(row);


